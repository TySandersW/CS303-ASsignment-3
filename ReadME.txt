This code runs the functions that were given by Assignment 3

In order to run the code, you can use the information already written into the code or you can change the code
to whatever the user needs to input. The code can also be changed so that the user can input the information
if they want but for simplicity sake I just made it with information already put in. 